"""
Model training and evaluation routines.

classical_baselines.py and fusion_model_eval.py (real Option A pipeline) are run as
standalone scripts/modules and import only what they need directly, rather than being
re-exported here, so importing this package doesn't require every training script's
dependencies to be installed at once.
"""
