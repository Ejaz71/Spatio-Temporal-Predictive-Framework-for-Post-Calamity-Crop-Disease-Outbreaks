# Core scientific computing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.2.0
statsmodels>=0.14.0

# Classical models + explainability
xgboost>=2.0.0
shap>=0.44.0
joblib>=1.3.0
matplotlib>=3.7.0  # calibration_plot.py, robustness_checks.py (decision curve)

# Fusion model (secondary — see results/fusion_model_results.json for why it's not primary)
torch>=2.0.0

# Real remote sensing / weather data acquisition
rasterio>=1.3.0
pystac-client>=0.7.0
planetary-computer>=1.0.0
requests>=2.28.0

# Optional alternative real-data pathway (data/gee_pipeline.py, needs GEE auth)
earthengine-api>=0.1.350

# Dashboard
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0

# Testing
pytest>=7.3.0
